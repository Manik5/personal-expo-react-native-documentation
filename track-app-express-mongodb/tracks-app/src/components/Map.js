import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Map = props => {
  return (
    <View>
      <Text>Map</Text>
    </View>
  )
};

const styles = StyleSheet.create({

});

export default Map;
